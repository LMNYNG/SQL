SELECT FISH_COUNT, MAX_LENGTH, FISH_TYPE
FROM ( SELECT COUNT(*) AS FISH_COUNT, MAX(LENGTH) AS MAX_LENGTH, FISH_TYPE, AVG(COALESCE(LENGTH,10)) AS AVERAGE
       FROM FISH_INFO
       GROUP BY FISH_TYPE  ) C
WHERE AVERAGE >= 33
ORDER BY FISH_TYPE ASC
