WITH FIRST AS (
SELECT ID
FROM ECOLI_DATA
WHERE PARENT_ID IS NULL
)
SELECT ID
FROM ECOLI_DATA
WHERE PARENT_ID IN (    SELECT A.ID
                        FROM ECOLI_DATA A, FIRST B
                        WHERE A.PARENT_ID=B.ID     )
