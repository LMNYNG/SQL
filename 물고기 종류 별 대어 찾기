SELECT ID, FISH_NAME, LENGTH
FROM FISH_INFO A JOIN FISH_NAME_INFO B ON A.FISH_TYPE=B.FISH_TYPE
WHERE (ID,LENGTH) IN (SELECT ID, MAX(LENGTH) OVER (PARTITION BY FISH_TYPE) 
                      FROM   FISH_INFO )
ORDER BY ID
